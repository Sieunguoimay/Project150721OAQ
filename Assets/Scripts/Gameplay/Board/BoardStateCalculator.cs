using System.Collections.Generic;
using System.Linq;

namespace Gameplay.Board
{
    public class BoardStateCalculator
    {
        // public void Calculate(int[] currentState, int tileIndex, bool direction, out int[] newState, out int[] eatenTileIndices, int totalEaten)
        // {
        //     var takenPieces = currentState[tileIndex];
        //     currentState[tileIndex] = 0;
        //     var currentIndex = tileIndex;
        //     for (var i = 0; i < takenPieces; i++)
        //     {
        //         currentIndex = BoardTraveller.MoveNext(tileIndex, currentState.Length, direction);
        //         currentState[currentIndex]++;
        //     }
        //     
        // }
    }
}